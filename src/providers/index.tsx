import Router from './router';
import TanstackQuery from './tanstackQuery';

const Providers = () => {
  return (
    <TanstackQuery>
      <Router />
    </TanstackQuery>
  );
};

export default Providers;
